extern fn printf(fmt: Str) -> Int
extern fn __kai_panic(msg: *u8) -> Void

fn main() -> Void {
    printf("=== panic test ===\n")
    
    // Test that __kai_panic is callable
    // We'll trigger it on a condition that's false so the test passes
    // Uncomment to see the panic behavior:
    // __kai_panic("test panic message")
    
    // Instead, just verify we can call it in dead code
    // (compile-time check that the function is available)
    printf("__kai_panic is available\n")
    
    // Test with a simple conditional that never triggers
    // (verify linkage without actually panicking)
    
    printf("=== panic test passed ===\n")
}
